interface:
  display_name: "Rust"
  short_description: "Repo-specific patterns and workflows for rust"
  default_prompt: "Use the rust repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true